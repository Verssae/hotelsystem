<!--
including header content which is common for all pages
-->
<%- include ../layouts/header.ejs %>	
		
<% if (messages.error) { %>
	<p style="color:red"><%- messages.error %></p>
<% } %>

<% if (messages.success) { %>
	<p style="color:green"><%- messages.success %></p>
<% } %>

<form action="/rooms/edit/<%= number %>" method="post" name="form1">
	Room Number
	<input type="text" name="number" value="<%= number %>" />
	<br/>
	Room Types
	<select name="type">
		<% if (data) { %>
			<% data.forEach(function(room_type){ %>
				<option value='<%= room_type.type %>' <% if (room_type.type == typed) { %>selected 
				<% } %>> <%= room_type.type %></option>
			<% }) %>
		<% } %>	
	</select>
	<br/>
	Room Floor
	<input type="text" name="floor" value="<%= floor %>" />
	<input type="submit" name="Submit" value="Edit" /></td>
	<input type="hidden" name="_method" value="PUT" />
</form>

<!--
including footer content which is common for all pages
-->
<%- include ../layouts/footer.ejs %>
